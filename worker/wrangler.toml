name = "znqm-api"
main = "index.js"
compatibility_date = "2024-09-23"
# 部署：wrangler deploy
# 配置密钥：wrangler secret put API_KEY   （填入 tokenhub.tencentmaas.com 的 Bearer Key）
# 该 Worker 处理 /api/* ，站点静态文件可托管在 GitHub Pages / Cloudflare Pages，
# 通过同域或自定义路由把 /api/* 指向本 Worker。
routes = [
  # 例：将 znqm.cloud/api/* 路由到本 Worker
  # { pattern = "znqm.cloud/api/*", custom_domain = true }
]
